node_modules
.next
data
.env.local
.env
*.tsbuildinfo
test-results
playwright-report
*.tmp.js

# Browser-review byproducts (throwaway): Playwright MCP page dumps and ad-hoc
# review screenshots at the app root. /*.png is anchored here, so reference
# photos under config/refs/ are unaffected.
.playwright-mcp
/*.png

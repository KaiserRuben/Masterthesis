# ═══════════════════════════════════════════════════════════════════════════
# EXP-27 — Cone-Alpha-Sweep, Pair B (junco↔chickadee), alpha = 20° (Exp-26)
# ═══════════════════════════════════════════════════════════════════════════

name: exp27_qwen_mps_pairB_cone20
save_dir: runs/Exp-27

modality: image_only

generations: 100
pop_size: 20

device: mps

sut:
  model_id: Qwen/Qwen3.5-4B
  backend: torch

cache_dirs:
  - ~/.cache/imagenet
  - /Volumes/SanDisk/Cache/imagenet

n_categories: 50

seeds:
  mode: roster
  filter_indices: [0]
  roster:
    class_list:
      - junco
      - chickadee
    seeds_per_class: 1
    min_anchor_confidence: 3.5
    abstraction:
      levels_anchor: [-1]
      levels_target: [-1]
      apply_disjointness: true
      directions: forward

image:
  backend: vqgan_codebook
  n_candidates: 16383
  knn_cache_path: ~/.cache/vqgan_knn/f8_16384_full.npz
  cone_filter:
    enabled: true
    alpha_deg: 20.0
    target_m: 10

optimizer:
  sampling:
    mode: sparse_multitier
    zero_anchor_fraction: 0.05
    tiers:
      - {p_active: 0.005, fraction: 0.20}
      - {p_active: 0.030, fraction: 0.20}
      - {p_active: 0.100, fraction: 0.25}
      - {p_active: 0.300, fraction: 0.20}
      - {p_active: 0.500, fraction: 0.10}
  early_stop:
    enable: true
    epsilon_margin: 1.0e-30
    plateau_patience: 50
    no_improvement_warmup: 30
    hypervolume_reference: null
